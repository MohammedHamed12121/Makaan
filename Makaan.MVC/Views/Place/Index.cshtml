@model List<Place>

@{
    Layout = "_AppLayout";
}

<div class="container-xxl py-5">
    <div class="container">
        <div class="row g-0 gx-5 align-items-end">
            @* <div class="col-lg-6">
            <div class="text-start mx-auto mb-5 wow slideInLeft" data-wow-delay="0.1s">
            <h1 class="mb-3">Property Listing</h1>
            <p>Eirmod sed ipsum dolor sit rebum labore magna erat. Tempor ut dolore lorem kasd vero ipsum sit eirmod sit
            diam justo sed rebum.</p>
            </div>
            </div> *@
            <div class="col-lg-6 text-start text-lg-end wow slideInRight" data-wow-delay="0.1s">
                <ul class="nav nav-pills d-inline-flex justify-content-end mb-5">
                    @* <li class="nav-item me-2">
                    <form asp-action="Index" method="get">
                    <button type="submit" class="btn btn-outline-primary active" data-bs-toggle="pill"
                    href="#tab-1">Featured</button>
                    </form>
                    </li>
                    <li class="nav-item me-2">
                    <form asp-action="Index" asp-route-status="@PlaceStatus.Sell" method="post">
                    <button type="submit" class="btn btn-outline-primary" data-bs-toggle="pill" href="#tab-2">For
                    Sell</button>
                    </form>
                    </li>
                    <li class="nav-item me-0">
                    <form asp-action="Index" asp-route-status="@PlaceStatus.Rent" method="post">
                    <button type="submit" class="btn btn-outline-primary" data-bs-toggle="pill" href="#tab-3">For
                    Rent</button>
                    </form>
                    </li> *@
                </ul>
            </div>
        </div>
        <div class="tab-content">
            <div id="tab-1" class="tab-pane fade show p-0 active">
                <div class="row g-4">

                    @foreach (var place in Model)
                    {

                        <partial name="_CardPartial" model="@place" />
                    }
                    <div class="col-12 text-center">
                        <a class="btn btn-primary py-3 px-5" href="">Browse More Property</a>
                    </div>
                </div>
            </div>

        </div>
    </div>
</div>
<!-- Property List End -->
